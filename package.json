{
  "name": "@beeswax/node-glfw",
  "version": "0.4.1",
  "scripts": {
    "setup": "cmake-js install",
    "build": "cmake-js build",
    "build:debug": "cmake-js build -D",
    "clean-build": "cmake-js clean && npm run build"
  },
  "types": "dist/index.d.ts",
  "dependencies": {
    "bindings": "^1.5.0",
    "bluebird": "^3.7.2",
    "cmake-js": "^6.2.1",
    "node-addon-api": "^4.1.0"
  }
}
